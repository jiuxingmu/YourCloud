# B组测试缺陷报告（TC-001~TC-009、TC-034、TC-042）

测试执行人：B  
执行日期：2026-04-29

## 1. 执行范围

- TC-001~TC-009（账号注册/登录/登出/鉴权）
- TC-034（创建分享失败-越权文件）
- TC-042（分享URL安全-忽略不可信Origin）

## 2. 自动化执行证据

### 2.1 后端（`services/api-go`）

执行命令：

- `go test ./...`
- `go test ./... -run 'Auth|Share|Token|Login|Register|Origin|ExtractCode|Forbidden|Unauthorized'`

关键结果：

- 两次执行均 `Exit code: 0`
- 关键包通过：`internal/handler`、`internal/service`
- 与本组强相关的已存在测试覆盖点：
  - 分享提取码鉴权（403/200）
  - 分享下载错误提取码（403）
  - 分享撤销后访问（410, `REVOKED`）
  - 分享创建忽略恶意 `Origin`，URL 使用请求 Host

### 2.2 前端（`clients/web`）

执行命令：

- `npm test`
- `npm test -- -t 'login|auth|logout|register|share|token'`

关键结果：

- 全量：`7 passed`, `47 passed`
- 聚焦：`4 passed | 3 skipped`, `10 passed | 37 skipped`
- 鉴权/分享相关测试通过（如 `apiClient.test.ts`、`App.test.ts`、`SharePage.test.ts`）

## 3. 缺陷结论

未发现阻断问题。

> 说明：基于当前可执行自动化测试，未出现失败用例或可复现的错误行为，故本次不新增 BUG-ID（B-001...）。

## 4. 用例执行结果（B组）

- `TC-001`：自动化部分覆盖，未发现异常（注册+登录服务层链路存在测试）
- `TC-002`：代码校验存在（前端邮箱格式校验），未发现自动化失败
- `TC-003`：代码校验存在（前端注册密码长度校验），未发现自动化失败
- `TC-004`：自动化部分覆盖，未发现异常（后端注册冲突逻辑存在）
- `TC-005`：自动化部分覆盖，未发现异常（登录服务链路通过）
- `TC-006`：自动化部分覆盖，未发现异常（前端401友好提示映射存在且测试通过）
- `TC-007`：代码校验存在（前端空字段拦截），未发现自动化失败
- `TC-008`：代码逻辑存在（登出清理 token），未发现自动化失败
- `TC-009`：本轮未直接自动化命中接口级无 token 断言，未见失败证据
- `TC-034`：后端越权分支逻辑存在（`forbidden` -> 403），未发现自动化失败
- `TC-042`：已被自动化命中并通过（忽略恶意 `Origin`）

## 5. 手工执行建议与阻塞点

### 5.1 建议补充的手工验证

以下用例建议在联调环境按文档步骤补跑，以补齐系统级证据：

- `TC-001~TC-009`：通过 Web 页面与 Network 面板核验提示文案、token 持久化、受保护接口鉴权行为
- `TC-034`：双账号（A/B）准备后，直接调用 `POST /api/v1/shares` 验证越权返回码与错误码
- `TC-042`：代理或 API 工具伪造 `Origin`，确认返回分享 URL 主机名基于请求 Host

### 5.2 当前阻塞点

- 当前会话中未具备稳定可用的本地联调服务实例（历史终端记录显示 `./scripts/dev.sh` 依赖 Docker 启动失败），因此未执行浏览器/API 级端到端手工步骤。
- 现有自动化测试以单测/组件测试为主，部分用例（特别是 `TC-009`、`TC-034` 的完整系统态）缺少直接命名测试用例映射，建议后续补充 API 层集成测试。
