# C组测试执行报告（TC-010 ~ TC-031）

- 执行人：测试执行人C
- 执行时间：2026-04-29
- 目标范围：文件主链路（上传/下载/预览/文件夹/移动/删除/回收站）

## 1. 执行结论（基于当前已完成证据）

本次按要求优先执行自动化验证。前端测试全部通过；后端 `go test ./...` 在依赖下载阶段长时间阻塞，按指令停止等待并中断进程。基于当前可得证据，**未发现可直接复现并确认的功能缺陷**，但存在后端自动化回归未完整跑通带来的覆盖盲区与风险。

## 2. 自动化执行证据

### 2.1 后端（`services/api-go`）

- 执行命令：`go test ./...`
- 状态：阻塞后中断（手动停止）
- 阻塞现象：持续停留在 Go module 下载阶段，未进入完整用例执行输出
- 已获取输出片段（关键）：
  - `go: downloading golang.org/x/image v0.39.0`
  - `go: downloading github.com/joho/godotenv v1.5.1`
  - `go: downloading gorm.io/gorm v1.25.12`
  - `go: downloading github.com/gin-gonic/gin v1.10.0`
- 阻塞处理：停止长耗时操作，终止进程 `pid=57502`

### 2.2 前端（`clients/web`）

- 执行命令：`npm test`
- 结果：通过（`7 files, 47 tests all passed`）
- 关键覆盖文件：
  - `src/pages/FilesPage.test.ts`（18 tests）
  - `src/features/files/application/fileActionsService.test.ts`（2 tests）
  - `src/components/FilePreview.test.ts`（7 tests）

## 3. TC-010~TC-031 覆盖评估

> 说明：以下为“自动化已覆盖/部分覆盖/未覆盖”判断，不等价于手工场景全通过。

- **上传与下载**
  - TC-010 上传成功：后端自动化未确认（阻塞）；前端无真实上传集成断言。
  - TC-011 中文空格文件名：未见直接自动化断言（盲区）。
  - TC-012 空文件上传：未见直接自动化断言（盲区）。
  - TC-013 大文件边界：未覆盖（盲区）。
  - TC-014 缺少 file 字段：未见直接自动化断言（盲区）。
  - TC-015 本人下载成功：后端存在 `FindDownloadByOwner` 正向测试（部分覆盖）。
  - TC-016 非法文件ID下载：前端有无效ID不可下载逻辑（部分覆盖）；后端返回码未确认。
  - TC-017 越权下载：后端存在 non-owner `record not found` 断言（部分覆盖）。

- **预览**
  - TC-018 图片预览成功：前端预览类型与拉取条件有单测（部分覆盖）；接口 200/mime 未实证。
  - TC-019 非图片预览失败：未见后端 422 `NOT_IMAGE` 自动化断言（盲区）。
  - TC-020 文件数据缺失：未见后端 404 `file data missing` 自动化断言（盲区）。

- **文件夹/移动/删除/回收站**
  - TC-021 新建文件夹成功：后端 handler/service 均有成功用例（覆盖较好）。
  - TC-022 空路径新建失败：未见直接断言（盲区）。
  - TC-023 路径穿越失败：后端 `CreateFolder("../escape")` 有失败断言（覆盖）。
  - TC-024 文件移动成功：后端 `MoveByOwner` 成功路径有覆盖（部分覆盖，偏目录重命名）。
  - TC-025 文件夹移动级联：后端有后代路径级联验证（覆盖）。
  - TC-026 目标路径空字符串：前端有 `validateMoveTargetName` 空白拦截（部分覆盖）；后端 400 未确认。
  - TC-027 目录移动到自身子目录：后端有 `gorm.ErrInvalidData` 断言（覆盖）。
  - TC-028 删除文件成功：后端删除路径覆盖到目录级联，普通文件单点行为未单测（部分覆盖）。
  - TC-029 删除文件夹级联：后端有 DB+存储双重清理断言（覆盖）。
  - TC-030 删除非法ID：未见直接断言（盲区）。
  - TC-031 回收站视图与清空：前端仅有“空/非空反馈文案”测试，缺少真实回收站列表与清空流程集成（部分覆盖）。

## 4. BUG列表

本轮在“当前已完成自动化证据”范围内，**未形成可确认的 BUG 复现闭环**，故未登记正式 BUG-ID。

## 5. 覆盖盲区与潜在缺陷风险（建议后续补测）

- 后端全量测试未跑完，`TC-010/011/012/013/014/018/019/020/022/026/030/031` 的接口语义仍需补证。
- 高风险边界仍缺自动化证据：大文件上传（TC-013）、文件数据缺失（TC-020）、回收站端到端清空（TC-031）。
- 前端现有测试偏“纯函数/展示逻辑”，对上传下载与回收站链路缺少 API 集成层断言，可能遗漏状态码到提示文案映射问题。

## 6. 建议下一步

1. 在网络可用环境重跑 `services/api-go` 的 `go test ./...`，并单独执行 file handler/service 相关测试以补齐证据。
2. 增加后端用例：`missing file`、`invalid id`、`not image`、`missing file field`。
3. 增加前端集成测试：FilesPage 上传/删除/回收站清空完整交互与错误码映射。
